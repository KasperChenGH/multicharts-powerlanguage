# Arw_SetTextColor

**Category:** Arrow_Drawing
**Signature:** `Arw_SetTextColor(ObjectID,TextColor)`

**Parameters**
- `ObjectID` *(numeric, required)* — see official docs
- `TextColor` *(numeric, required)* — see official docs

**Example (illustrative)**
```
Arw_SetTextColor( ObjectID, TextColor );  // parameter names are placeholders -- replace with real values (see the Usage line above)
```

*Official docs:* https://www.multicharts.com/trading-software/index.php?title=Arw_SetTextColor
